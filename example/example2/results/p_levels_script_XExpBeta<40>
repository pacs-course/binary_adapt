#It produces the plot of the plevels of the binary refined mesh
#call it by typing in gnuplot the instruction:
#	call "results/p_levels_script_XExpBeta<40>"
p_0(x) = x > 0.000000 && x <= 1.000000? 40 : 1/0
set xrange [0:1]
set yrange [-1:41]
set xlabel "#iter"
set samples 1000
set xtics nomirror
set x2tics ("" 0, \
"" 1)
set grid noxtics noytics x2tics
plot p_0(x)
