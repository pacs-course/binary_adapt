Compute the whole basis at the start basing on the iteration number

CoefficientComputer calls OneDEvaluation, which is unefficient

rimuovere i template da coefficientcomputer, sostituirli con il polimorfismo. Verificare se FeType e' necessario come parametro del template

rendere il codice indipendente dalla libreria che calcola jacobi

nel plugin loading la navigazione ricorsiva delle subdirectories puo' essere deleteria

example linka direttamente i file (non dipende quindi da install), ma poi il codice del main ha bisogno dell'installazione

per la visualizzazione tradurre: https://sourceforge.net/p/octave/fpl/ci/default/tree/inst/fpl_vtk_raw_write_field.m

verificare se conviene scrivere i polinomi sfruttando il fatto di conoscerne gli zeri dalle regole di quadratura

in documentazione specificare: - l'ordine in cui linkare le librerie
										 - di compilare sandia_rules e jacobi_polynomials con il flag -fPIC
